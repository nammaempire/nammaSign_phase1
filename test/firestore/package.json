{
  "name": "reset95-firestore-rules-test",
  "private": true,
  "description": "Security-rules tests for ../../firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --only firestore --project reset95-rules-test \"jest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.2",
    "firebase-tools": "^13.20.2",
    "jest": "^29.7.0"
  }
}
